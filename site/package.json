{
  "name": "@manta/site",
  "version": "0.1.0",
  "description": "The Manta language landing page — deployed on Vercel.",
  "scripts": {
    "dev": "npx serve public -p 3000",
    "build": "echo 'Static site — no build step required.'",
    "start": "npx serve public"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
